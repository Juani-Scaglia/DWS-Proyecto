// Package clients define los clientes para consumir servicios externos
// (APIs de terceros, servicios de autenticación, bases de datos no relacionales, etc.).
// En la versión actual del sistema no se integran servicios externos,
// por lo que este paquete está reservado para futuras extensiones.
package clients
